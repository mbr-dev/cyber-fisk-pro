import { Container } from "./styles"

export const LineSeparator = ({ w, mt }) => {
  return (
    <Container
      style={{
        width: w,
        marginTop: mt
      }}
    >
    </Container>
  )
}