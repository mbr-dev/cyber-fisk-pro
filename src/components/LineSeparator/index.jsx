import { Container } from "./styles";

export const LineSeparator = ({ w, bg }) => {
  return (
    <Container
      style={{
        width: w,
        backgroundColor: bg
      }}
    >
    </Container>
  )
}