import { Container } from "./styles";

export const LineSeparator = ({ wl, bg }) => {
  return (
    <Container
      style={{
        width: wl,
        backgroundColor: bg,
      }}
    >
    </Container>
  )
}